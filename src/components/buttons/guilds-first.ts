import { Button } from '../../types/button';

const button: Button = {
    data: {
        name: 'guilds-first',
    },
    async execute(interaction) {
        return;
    },
};

export default button;
