import { Button } from '../../types/button';

const button: Button = {
    data: {
        name: 'guilds-last',
    },
    async execute(interaction) {
        return;
    },
};

export default button;
