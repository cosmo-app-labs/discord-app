import { Button } from '../../types/button';

const button: Button = {
    data: {
        name: 'guilds-next',
    },
    async execute(interaction) {
        return;
    },
};

export default button;
