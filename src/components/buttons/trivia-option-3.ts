import { Button } from "../../types/button";

const button: Button = {
    data: {
        name: "trivia-option-3",
    },
    async execute(interaction) {
        return;
    },
};

export default button;
