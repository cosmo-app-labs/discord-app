import { Button } from '../../types/button';

const button: Button = {
    data: {
        name: 'guilds-previous',
    },
    async execute(interaction) {
        return;
    },
};

export default button;
