import { Button } from '../../types/button';

const button: Button = {
    data: {
        name: 'trivia-option-4',
    },
    async execute(interaction) {
        return;
    },
};

export default button;
